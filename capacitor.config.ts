import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eminds.ionic.crud.demo',
  appName: 'ionic-crud-demo',
  webDir: 'www'
};

export default config;
